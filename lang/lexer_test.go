package lang

import (
	"testing"

	// "github.com/stretchr/testify/assert"
)

func TestLexer(t *testing.T) {
	t.Run("Process simple command", func(t *testing.T) {
		// assert := assert.New(t)
		// sample := "ping hello world"
	})
}
